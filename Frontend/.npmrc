# Configuración de npm para resolver conflictos de dependencias
legacy-peer-deps=true

# Configuración de registro (opcional)
# registry=https://registry.npmjs.org/

# Guardar exactamente la versión instalada (opcional)
save-exact=true
